# 提交前仅对「已暂存」文件执行 eslint --fix / prettier --write（约 3s）。
# 全仓库 format:check、typecheck、api:handlers:check 放在 pre-push，
# 避免 Cursor 内置 Git 提交因钩子过重（全量 prettier + tsc）触发 SIGKILL（signal 9）。
pnpm exec lint-staged
