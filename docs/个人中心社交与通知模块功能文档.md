# 个人中心社交与通知模块功能文档

> 文档状态：**正式功能文档（基于当前代码实现）**
>
> 适用范围：个人中心中与「粉丝、关注、通知」相关的前后端能力与页面交互。
>
> 更新时间：2026-04-22

## 1. 目标与范围

本模块用于将个人中心从“数据展示”升级为“可操作入口”，覆盖以下能力：

- 统计卡片快捷跳转（粉丝 / 关注 / 未读通知）
- 粉丝与关注关系页（列表、筛选、分页、操作）
- 通知中心任务化处理（筛选、标记、删除、主动作 CTA、回关）

不包含：

- 通知“清理已读”后端接口与前端动作（当前未实现）
- 通知列表“加载更多 / 分页”前端交互（当前为固定批量拉取）

## 2. 实现状态总览（按原方案 T1-T7）

| 任务                         | 状态               | 说明                                       |
| ---------------------------- | ------------------ | ------------------------------------------ |
| T1 统计卡片跳转行为接入      | 已实现             | 三张关键卡片可点击并按语言路由跳转         |
| T2 粉丝页路由与骨架          | 已实现             | 已升级为真实接口驱动，保留 fallback 兜底   |
| T3 关注页路由与骨架          | 已实现             | 已升级为真实接口驱动，保留 fallback 兜底   |
| T4 通知页 URL 初始筛选与 CTA | 已实现             | 支持 `status` 初始筛选与类型化主动作       |
| T5 关系接口联调              | 已实现             | followers/following/follow/unfollow 已落地 |
| T6 通知批量能力补强          | 部分实现           | 已有“全部已读”；“清理已读”“加载更多”未落地 |
| T7 三语文案补齐              | 已实现（当前形态） | 主要在组件内三语对象完成，非统一字典键     |

## 3. 前端实现清单

### 3.1 统计卡片入口（T1）

- `components/profile/profile-stats.tsx`
  - `totalFollowers` -> `/{lang}/profile/followers`
  - `totalFollowing` -> `/{lang}/profile/following`
  - `unreadNotifications` -> `/{lang}/profile/notifications?status=unread`
  - 已补充 hover / focus 样式与可访问性 `aria-label`

### 3.2 粉丝页（T2 + T5）

- 路由：`app/[lang]/profile/followers/page.tsx`
- 组件：`components/profile/profile-followers.tsx`
- 关键能力：
  - 搜索、仅互关筛选、分页
  - 回关操作（调用真实接口）
  - 查看主页跳转
  - 接口失败 fallback 与提示去重
  - loading / empty / normal / fallback 四态

### 3.3 关注页（T3 + T5）

- 路由：`app/[lang]/profile/following/page.tsx`
- 组件：`components/profile/profile-following.tsx`
- 关键能力：
  - 搜索、仅互关筛选、分页
  - 取消关注（调用真实接口）
  - 查看主页跳转
  - 接口失败 fallback 与提示去重
  - loading / empty / normal / fallback 四态

### 3.4 通知页（T4 + T6 部分）

- 路由：`app/[lang]/profile/notifications/page.tsx`
- 组件：`components/profile/profile-notifications.tsx`
- 关键能力：
  - 支持 URL 初始筛选：`status=unread|read`
  - 类型筛选、已读状态筛选
  - 单条已读、批量全部已读、单条删除
  - 主动作 CTA（按通知类型）
  - follow 类型支持“回关”直连（调用关注接口）

## 4. 后端接口实现清单

### 4.1 关系接口（T5）

- `GET /api/profile/followers`
  - 文件：`app/api/profile/followers/route.ts`
  - 支持：`page`、`limit`、`search`、`mutualOnly`
- `GET /api/profile/following`
  - 文件：`app/api/profile/following/route.ts`
  - 支持：`page`、`limit`、`search`、`mutualOnly`
- `POST /api/profile/follow`
  - 文件：`app/api/profile/follow/route.ts`
  - 关注用户，含重复关注/自关注校验，并尝试写入 follow 通知
- `DELETE /api/profile/follow/[targetUserId]`
  - 文件：`app/api/profile/follow/[targetUserId]/route.ts`
  - 取消关注

### 4.2 通知接口（既有 + 本轮联动）

- `GET /api/notifications`：列表（含筛选与分页参数）
- `PUT /api/notifications`：批量已读
- `PUT /api/notifications/[id]`：单条已读等更新
- `DELETE /api/notifications/[id]`：单条删除

## 5. 数据类型与请求层

- 类型：
  - `types/blog.ts`
  - `ProfileRelationItem`
  - `ProfileRelationListResponse`
- 请求层：
  - `lib/api/profile-relations.ts`
  - `getFollowers` / `getFollowing` / `followUser` / `unfollowUser`

## 6. 与原临时方案差异说明

1. 原方案中 T5 标注“需后端支持”，当前已全部落地。
2. 原方案中 T7 计划走 `dictionaries/*.json`，当前实现为组件内三语文案对象，用户可见效果已具备。
3. 原方案中 T6 的“清理已读”和“加载更多”仍为待补强项。

## 7. 待办（下一步建议）

- 通知中心补充“清理已读”接口与前端按钮联动。
- 通知列表支持“加载更多”或分页，替代当前固定 `limit=100` 拉取。
- 将 profile 社交通知相关文案逐步收敛到 `dictionaries/*.json`，减少组件内硬编码翻译对象。
