# 变更记录

本文件集中记录仓库级变更约定与重要变更。**新增条目一律写在本文最上方**（时间倒序），每一块采用 **日期 — 标题** 为二级标题；不必为每次变更单独新建 `变更记录-*.md` 文件。

---

## 2026-04-17 — 分类/标签重构：用户级隔离与前端管理入口收敛

**类型**：refactor（web / api / db / docs）

### 处理

- 分类/标签 API 统一改为登录必需，读写按 `ownerId` 隔离到当前用户。
- `categories`、`tags` 表新增 `owner_id` 及索引，`seed/test` 脚本同步适配。
- 分类页与标签页加入页内管理能力（创建、编辑、删除、状态切换），并统一 Bearer 请求。
- 用户菜单入口从“分类/标签浏览”调整为“分类管理/标签管理”，指向 `/{lang}/categories/manage` 与 `/{lang}/tags/manage`。
- 同步更新分类/标签、接口与数据表文档（`docs/分类与标签界面说明.md`、`docs/接口文档.md`、`docs/接口变更文档.md`、`docs/数据库表记录文档.md`、`docs/数据表变更文档.md`）。

---

## 2026-04-17 — 去兼容化清理：移除 `userId=0` 历史分支

**类型**：refactor（auth / api）

### 处理

- 超级管理员身份统一为真实 DB 用户，认证链路不再保留 `userId=0` 特判。
- 点赞/收藏/engagement 删除历史占位/403 分支，登录用户统一走真实关系表逻辑。
- 注册与资料相关邮箱冲突校验改为仅按真实超管资料行判定。

---

## 2026-04-17 — 超级管理员登录迁移到真实 DB 用户（兼容旧 token）

**类型**：fix（auth / data-compat）

### 处理

- 超管登录成功后自动确保 `users` 超管账号存在并签发真实 `userId`；刷新接口兼容 `userId=0` 旧 token 并升级为真实 `userId`。
- 首次登录自动将 `user_profiles.user_id=<super_admin_id>` 迁移复制到真实超管 `user_id`（若目标行不存在）。
- 互动接口的 限制仅作用于 `userId=0`，DB 超管恢复点赞/收藏。

---

## 2026-04-17 — 文章点赞/收藏/engagement 接口 500 与超级管理员约束修复

**类型**：fix（api / auth）

### 处理

- 见 **`docs/接口变更文档.md`** 同日条目；涉及 JWT `userId` 校验、`GET /api/posts/engagement`、`POST` 点赞与收藏、**`toJsonSafeInt`** 与路由侧错误日志。

---

## 2026-04-17 — 接口/库表文档清单与 Cursor 同步规则

**类型**：docs + chore（api-docs meta）

### 处理

- **新增** **`.cursor/rules/接口与数据库文档同步.mdc`**：约定新增或变更 `app/api`、修改 `lib/db/schema.ts` 时，按需更新 **`lib/utils/api-docs-endpoint-meta.ts`**、**`docs/接口文档.md`**、**`docs/接口变更文档.md`**、**`docs/数据库表记录文档.md`**、**`docs/数据表变更文档.md`**。
- **新增** **`docs/接口文档.md`**（全量接口表）、**`docs/接口变更文档.md`**、**`docs/数据库表记录文档.md`**、**`docs/数据表变更文档.md`**。
- **修正** **`lib/utils/api-docs-endpoint-meta.ts`**：与当前 Route Handler 对齐（`admin/users` 内存超管、`about/owner` 仅 GET、补充 `posts` engagement/like/favorite、`uploads` DELETE、`profile/activities` POST 等）。
- **更新** **`docs/文档索引.md`**。

---

## 2026-04-16 — API 文档站：超级管理员门禁、鉴权测试与元数据补全

**类型**：feat（api / web / docs）

### 处理

- **`app/api/api-docs/route.ts`**：`GET` 增加 **`requireInMemorySuperRoot`**；响应体附带 **`stats`**；非授权返回 **401/403**。
- **`lib/utils/authz.ts`**：新增 **`isInMemorySuperRootClientUser`**（与 JWT 约定对齐，供前端门禁）。
- **`components/api-docs`**：`ApiDocsAccessDenied`、`ApiDocsTesterProvider`、`ApiDocsAuthPanel`（登录测试 + 同步全站 Token）；**`api-docs-client`** 门禁 + 拉取数据优先上下文 Bearer；**`api-tester`** 自动/手动填入文档 Token。
- **`lib/utils/api-docs-endpoint-meta.ts`**（新）：集中维护分组/端点/鉴权说明；**`lib/utils/api-scanner.ts`** 增加 **`authHint`** 字段并合并元数据。
- **`components/api-docs/api-endpoint-card.tsx`**：展示鉴权提示。
- **`app/[lang]/api-docs/page.tsx`**：向客户端传入 **`lang`**；**`dictionaries/*`** 更新 `apiDocs.description`。
- **文档**：**`docs/接口文档系统说明.md`** 重写访问控制、文件职责与使用方式。

---

## 2026-04-16 — 访客邮件订阅/退订：邮箱验证码校验

**类型**：feat（api / web / db）

### 处理

- **`lib/db/schema.ts`**：`email_verifications.type` 枚举新增 **`subscription`**、**`subscription_unsubscribe`**；配套 **`pnpm db:generate`** 生成迁移（如 **`drizzle/0004_*.sql`**），部署需 **`pnpm db:migrate`**（或团队等价流程）。
- **`app/api/auth/send-verification-code/route.ts`**：支持上述类型；订阅类校验「已订阅 / 未订阅」后再发信，避免对任意邮箱滥发退订码。
- **`lib/utils/email.ts`**：订阅/退订验证码邮件模板。
- **`lib/services/email-verification-consume.ts`**：统一「校验并一次性消费验证码」逻辑。
- **`app/api/subscriptions/route.ts`**：未带有效 JWT 时，`POST` 须 **`verificationCode`（类型 `subscription`）**、`DELETE` 须 **`verificationCode`（类型 `subscription_unsubscribe`）**；已登录须 **`Authorization: Bearer`** 且 **`email` 与账号邮箱一致**（不再信任 body 中的 `userId`）。
- **`types/blog.ts`**：`CreateSubscriptionRequest` 增加可选 **`verificationCode`**。
- **`lib/hooks/useNewsletterGuestSubscription.ts`** + **`components/layout/footer.tsx`** + **`components/blog/blog-sidebar.tsx`**：访客展示邮箱、验证码、「发送验证码」与主按钮；访客订阅状态由 **`GET /api/subscriptions`** 防抖查询。
- **文档**：**`docs/邮件订阅与发布通知功能说明.md`** 同步 API 与前端行为。

---

## 2026-04-12 — 个人中心 UI 与博客前台对齐

**类型**：refactor（web）

### 处理

- **`components/profile/profile-layout.tsx`**：采用与 **`BlogPageContent`** 一致的 **`min-h-screen` 渐变背景**、**`container` + 栅格**；移除独立顶栏；侧栏置于大屏右侧 **`lg:col-span-1`**；小屏仅保留「目录」打开抽屉。
- **`components/profile/profile-sidebar.tsx`**：玻璃卡片导航、**`useAuth`** 展示头像/昵称/邮箱；**`variant`** 区分内嵌与抽屉；导航项仅保留已上线子路由（概览、文章、收藏、通知、设置）。
- **`components/profile/profile-ui-presets.ts`**：抽取 **`PROFILE_GLASS_CARD`** 等与博客统一的表面样式。
- **各子模块**（**`profile-overview` / `stats` / `activities` / `posts` / `favorites` / `notifications` / `settings` / `loading`**）：卡片与文案色改为 **`foreground` / `default-*`**，筛选区原生控件与博客输入风格协调；**`profile-stats`** 快捷入口改为 **`Link`**；**`profile-posts`**「写文章」链至 **`/{lang}/blog/manage/create`**。
- **删除**：**`components/profile/profile-navigation.tsx`**（与全站 Header 重复）。
- **文档**：**`docs/用户中心实现说明.md`** 组件列表与设计说明同步。

---

## 2026-04-12 — 博客侧栏：分类/标签跳转列表（blog?categoryId / blog?tagId）

**类型**：feat（web）

### 处理

- **`components/blog/blog-sidebar.tsx`**：分类链接改为 **`/{lang}/blog?categoryId=`**；热门标签改为 **`/{lang}/blog?tagId=`**（不再指向 **`/categories/[slug]`**、**`/tags/[slug]`**）。
- **`app/[lang]/blog/page.tsx`**：解析 **`tagId`** 查询串并传入 **`BlogPageContent`**。
- **`components/blog/blog-page-content.tsx`**：首屏 **`initialParams`** 可带 **`tagId`**；标签说明卡片（与分类卡片风格一致）；地址栏与 **`categoryId` / `tagId`** 同步的 **`useEffect`** 扩展为同时处理二者及双参组合。
- **`lib/hooks/usePosts.ts`**：新增 **`filterByTag`**；**`filterByCategory`** 时一并清除 **`tagId`**，避免互斥筛选残留。

---

## 2026-04-12 — 首页分类跳转至博客列表（categoryId 查询串）

**类型**：feat（web）

### 处理

- **`app/[lang]/page.tsx`**：首页「分类」卡片链接由 **`/categories`** 改为 **`/blog?categoryId={id}`**。
- **`app/[lang]/blog/page.tsx`**：改为服务端页面，读取 **`searchParams.categoryId`** 并传入客户端。
- **`components/blog/blog-page-content.tsx`**（新）：原博客列表客户端逻辑；**`usePosts`** 首屏 **`initialParams`** 可带 **`categoryId`**；分类切换时 **`router.replace`** 同步地址栏；带分类时顶部展示分类说明卡片与「查看全部文章」。

---

## 2026-04-12 — 博客正文 Markdown 展示同步与摘要剥离

**类型**：fix / style（web）

### 处理

- **`components/blog/markdown-renderer.tsx`**：与 **`SimpleEditor`** 一致，在 Viewer 就绪后 **`getInstance().setMarkdown`** 同步 **`content`**，避免仅 **`initialValue`** 在异步加载或切换文章后不更新；外层 **`article.blog-article-markdown`**；加载态改用 HeroUI **`Spinner`**。
- **`lib/utils/markdown-plain.ts`**：新增 **`stripMarkdownForExcerpt`**，列表/卡片/详情副标题中的摘要去掉常见 Markdown 标记，避免原样露出 `**`、`#` 等。
- **列表与摘要展示**：**`PostCard`**、**`app/[lang]/page.tsx`** 首页卡片、**`blog/manage`**、**`blog/[slug]`** 详情副标题、**`profile-posts` / `profile-favorites`** 使用上述工具。
- **`styles/toast-ui-editor.scss`**：**`.blog-article-markdown`** 阅读宽度与字号/行高微调。

---

## 2026-04-12 — 部署：db-migrate 拆分构建/运行、BuildKit 与 Actions 超时

**类型**：fix / build（root）

### 处理

- **`.github/workflows/deploy.yml`**：远端导出 **`DOCKER_BUILDKIT=1`**、**`COMPOSE_DOCKER_CLI_BUILD=1`**，使 **`Dockerfile.migrate`** 的 pnpm store **BuildKit 缓存**生效；将 **`db-migrate`** 拆为 **step2a `compose build`**（默认 **3600s**）与 **step2b `compose run`**（**600s**），避免 **`run --build`** 单条超时同时覆盖「极慢的依赖安装」与「极短的 migrate」；部署 **job** 的 **`timeout-minutes`** 调整为 **120**。
- **`Dockerfile.migrate`**（沿用）：**`# syntax=docker/dockerfile:1`** + **`pnpm install`** 的 **`RUN --mount=type=cache`**。
- **`docs/Docker编排与流水线部署.md`**：同步流水线步骤与 **124** 排障说明。

### 说明

- 同日早前曾将 **`run --build`** 单步 **`timeout`** 放宽至 **1200s**；在 **`reused:0`、全量下载** 场景下仍可能 **124**，故改为拆分与上述环境变量。

---

## 2026-04-11 — env 模板：本地与 deploy 职责说明及 MinIO 键补全

**类型**：docs / 配置模板

### 处理

- **`env.example`**：文首说明与 **`deploy/env.docker.example`** 键集合不必一致的原因；列出 **`pnpm dev`** 侧 MinIO 所需键（含 **`MINIO_ROOT_*` 回退**、**`NEXT_PUBLIC_*`**）。
- **`deploy/env.docker.example`**：文首补充与 **`env.example`** / **`blog-web`** 内 **`MINIO_ENDPOINT`** 的分工说明。

---

## 2026-04-11 — 博客特色图：MinIO 上传与 article/profile 对象键

**类型**：feat（web）

### 处理

- 依赖 **`@aws-sdk/client-s3`**；**`lib/services/object-storage.service.ts`**、**`lib/storage/resource-scopes.ts`**、**`lib/utils/request-auth.ts`**、**`lib/utils/public-object-url.ts`**。
- **API**：**`POST/DELETE /api/uploads/image`**（登录、multipart、`scope`=article|profile、可选 `previousKey` 替换删旧）。
- **UI**：**`FeaturedImageUpload`** 接入 **`blog/manage/create`**、**`edit/[id]`**；**`posts.featured_image`** 扩至 **512**（**`drizzle/0002_*`**）。
- **配置**：**`env.example`**、**`deploy/env.docker.example`**、**`docker-compose.yml`**（**`blog-web`** 注入 **`MINIO_ENDPOINT`** 与 **build args**）、**`Dockerfile`**（**`NEXT_PUBLIC_MINIO_*` ARG**）、**`next.config.ts`**（**`127.0.0.1:19000`** 远程图）。
- **文档**：**`docs/博客特色图与MinIO上传.md`**、**`docs/文档索引.md`**。

---

## 2026-04-11 — macOS：MinIO 挂载路径与 Docker File Sharing

**类型**：文档

### 处理

- **`deploy/env.docker.example`**、**`docs/Docker编排与流水线部署.md`**（常见问题）：说明勿在 macOS Docker Desktop 下使用 **`/data/minio`** 等未共享根路径，避免 **`mounts denied`**；推荐 **`./data/minio`** 或用户目录下绝对路径。

---

## 2026-04-11 — MinIO 数据目录环境变量 MINIO_HOST_DATA_DIR

**类型**：运维 / 编排

### 处理

- **`docker-compose.yml`**：`minio` 改为 **`bind`** 挂载 **`${MINIO_HOST_DATA_DIR:-./data/minio}:/data`**，移除命名卷 **`minio_data`**。
- **`deploy/env.docker.example`**：新增 **`MINIO_HOST_DATA_DIR`**（默认 **`./data/minio`**）及相对路径说明。
- **`docs/Docker编排与流水线部署.md`**、**`.github/workflows/deploy.yml` 注释**：同步「数据在绑定目录」表述；说明自旧版 **`minio_data`** 迁移时注意拷贝数据。

---

## 2026-04-11 — MinIO 绑定宿主机目录说明

**类型**：文档 / 编排注释

### 处理

- **`docker-compose.yml`**：`minio` 服务 **`volumes`** 处补充「命名卷 vs 绑定宿主机路径」注释与 **`./data/minio:/data`** 示例。
- **`.gitignore`**：忽略 **`/data/minio/`**（选用仓库内绑定目录时）。
- **`docs/Docker编排与流水线部署.md`**：常见问题表增加对应一行。

---

## 2026-04-11 — env.example 与当前代码对齐

**类型**：配置 / 文档

### 处理

- **`env.example`**：删除当前代码未读取的项（`GOOGLE_*`、`REDIS_URL`、`CACHE_TTL`、`CORS_ORIGIN`、`RATE_LIMIT_*`、本地上传占位、`MINIO_*` 注释块）；保留 **`DB_*`、`NODE_ENV`、`NEXT_PUBLIC_APP_URL`、`JWT_*`、`SMTP_*`**。
- **`docs/Docker编排与流水线部署.md`**、**`docs/博客系统开发说明.md`**、**`docs/功能清单与路线图.md`**：同步说明「本地模板 vs `deploy/env.docker.example`」分工。

---

## 2026-04-11 — Docker Compose 集成 MinIO 与流水线幂等初始化

**类型**：运维 / 部署

### 处理

- `docker-compose.yml`：新增 **`minio`**（卷 **`minio_data`**）、**`minio-init`**（profile **`minio-init`**，`mc` 创建 **`MINIO_BUCKET`** 并 `anonymous set download`，适合公开文章配图）。
- `deploy/env.docker.example`、`env.example`：补充 MinIO 相关变量与说明；**已有服务器**需在 **`deploy/.env.docker`** 手工追加 **`MINIO_ROOT_USER` / `MINIO_ROOT_PASSWORD`** 等键。
- `.github/workflows/deploy.yml`：`up -d` 包含 **`minio`**，并在迁移前执行 **`minio-init`**（超时 120s，失败则部署失败）。
- `package.json`：**`docker:up:deps`** 含 **`minio`**；新增 **`docker:minio:init`**。
- `docs/Docker编排与流水线部署.md`：端口表、启动顺序、流水线步骤、**§6.0**（推 main 与 MinIO 数据持久化说明）、常见问题；`docs/文档索引.md` 部署说明一行同步。

---

## 2026-04-11 — 部署流水线：db-migrate 强制 --build

**类型**：CI / 运维

### 处理

- `.github/workflows/deploy.yml`：`docker compose ... run ... db-migrate` 增加 **`--build`**，避免沿用旧镜像内已 `COPY` 的 `drizzle/`，导致新增迁移文件（如 `0001_*`）从未进入迁移容器、库表不更新却显示 migrate 成功。
- `package.json` 的 **`docker:migrate`**、`docker-compose.yml` 顶部注释、`docs/Docker编排与流水线部署.md`：同步建议/使用 **`--build`**，并在常见问题表中补充「迁移成功但无新表」排查。

---

## 2026-04-11 — 邮件订阅：生产 migrate 后仍失败的排障说明

**类型**：文档

### 处理

- `docs/邮件订阅与发布通知功能说明.md`：新增 **§3.3**，说明需确认迁移成功退出码、**迁移目标库与 Next 实际 `DB_*` 一致**、仓库含 `drizzle/0001_*` 等常见原因与自检命令。

---

## 2026-04-11 — 浅色首页雾气朦胧动效

**类型**：前端 / 样式

### 处理

- 新增 `components/home/home-light-mist.tsx`（Client）：`resolvedTheme === "light"` 时渲染；与深色 `HomeDarkStarfield` 互斥。
- `styles/globals.scss`：三层径向渐变「雾团」+ 底层柔光 + 横向淡雾带（`background-position` 慢移）；仅用 `transform` / `opacity` / `background-position`，避免全屏 `filter: blur`；`contain: layout style paint`；`prefers-reduced-motion` 下停动画。
- `app/[lang]/page.tsx`：在 `HomeDarkStarfield` 旁挂载 `HomeLightMist`（同 `z-[1]` 层）。

---

## 2026-04-11 — 首页深色主题 Canvas 星野粒子

**类型**：前端

### 处理

- 新增 `components/home/home-dark-starfield.tsx`（Client）：`resolvedTheme === "dark"` 时在首页主区域铺满 Canvas，粒子自中心向外飞散；浅色主题不挂载。
- `prefers-reduced-motion: reduce` 时仅绘制静态低密度粒子，不跑 rAF 循环。
- `app/[lang]/page.tsx`：在 `HomeAmbientBackground` 之上、`z-10` 内容之下插入该层。

---

## 2026-04-11 — 首页背景氛围动效（极光 + 噪点 + 光带）

**类型**：前端 / 样式

### 处理

- 新增 `components/home/home-ambient-background.tsx`，在 `app/[lang]/page.tsx` 的 `<main>` 内作为底层绝对定位层渲染。
- `styles/globals.scss`：极光三色光晕（长周期 transform + opacity）、SVG 噪点叠层（极低透明度脉动）、宽光带 `background-position` 慢扫；`prefers-reduced-motion: reduce` 时关闭动画并保留静态层次。

---

## 2026-04-11 — 博客首页产品化改版（去技术栈、真实数据、响应式）

**类型**：前端 / 国际化文案

### 需求

- 首页不再展示「博客搭建技术栈」等与读者无关的实现细节。
- 从阅读类产品常见结构出发：品牌首屏、主题入口、最新内容、价值说明与行动号召。
- 响应式适配移动端，版式与留白更易读。

### 处理

- `app/[lang]/page.tsx`：移除模拟数据、统计块、技术栈与偏营销的「Why us」技术表述；改为 `postService.getPosts` 拉取已发布公开文章、Drizzle 查询激活分类；首篇在宽屏占双列并采用横版卡片；封面支持任意 URL（`next/image` + `unoptimized`）。
- `dictionaries/zh-CN.json`、`en-US.json`、`ja-JP.json`：新增 `home` 键，承载首页区块文案。
- 分类区在窄屏为横向滑动（隐藏滚动条），宽屏为换行卡片。

### 验证

- `pnpm exec tsc --noEmit` 通过。

---

## 2026-04-10 — 邮件订阅与文章发布通知功能

**类型**：功能 / API / 邮件通知

### 需求

- 未登录用户可输入邮箱订阅更新。
- 已登录用户使用注册邮箱订阅/取消订阅，不显示邮箱输入框。
- 同一邮箱不可重复订阅。
- 文章发布时向所有有效订阅邮箱发送文章简介与文章链接。

### 处理

- 数据层：新增 `email_subscriptions`（`email` 唯一、`isActive`、`subscribedAt`、`unsubscribedAt`、`userId` 可选）。
- API：新增 `app/api/subscriptions/route.ts`，提供状态查询（GET）、订阅（POST）、取消订阅（DELETE）。
- 前端：改造 `components/blog/blog-sidebar.tsx` 的订阅卡片：
  - 未登录显示邮箱输入框 + 订阅按钮；
  - 已登录仅显示按钮，并根据订阅状态切换“订阅/取消订阅”。
- 通知链路：新增 `lib/services/subscription.service.ts` 与 `sendPostPublishedEmail`，在：
  - `app/api/posts/route.ts`（新建即发布）
  - `app/api/posts/[id]/route.ts`（PUT/PATCH 从未发布到已发布）
    触发订阅通知发送。
- 修复发布触发判断：兼容文章服务返回嵌套结构（`posts` 子对象）。
- 增强日志：输出邮件发送成功/失败与批量发送汇总。

### 验证

- 已实测匿名订阅、登录订阅、重复订阅防重、取消订阅、发布触发通知、草稿转发布触发通知、取消订阅后不再通知。
- 迁移流程：`pnpm db:generate && pnpm db:migrate`。

---

## 2026-04-10 — 脚本与提示统一为 Compose + Drizzle

**类型**：脚本 / 文档

### 处理

- **`scripts/deploy-from-scratch.sh`**：改为 **docker compose** 启动 **mysql/redis**、**`--profile migrate`** 跑 **Drizzle**；同步 **`.env.local`** 宿主机端口；去掉裸 **`docker run`** 与固定本机卷路径。
- **`scripts/check-deployment-status.sh`**：按 Compose 检查容器；表数量阈值改为 **`>=10`**；文案指向 Docker 文档与从零脚本。
- **`scripts/backup-production.sh`**：注释头说明 **mysqldump 仅为备份**，建表仍走 Drizzle。
- **`scripts/test-db-connect.ts`**、**`scripts/setup-env.ts`**、**`app/api/test-db/route.ts`**、**`app/api/test-env/route.ts`**：错误提示与配置指南改为 **Drizzle / docker:migrate**，去掉「手写 SQL 建库」为主路径的表述。
- **`docs/数据库重置与验证指南.md`**：补充「仅以 Drizzle 建表」一句。
- **`docs/文档索引.md`**、**`README.md`**：补充从零脚本与状态检查脚本的入口说明。

---

## 2026-04-10 — 分类文档合并与移除 init-db

**类型**：文档 / 数据库流程

### 处理

- 将 **`docs/分类实现摘要.md`** 并入 **`docs/分类管理说明.md`**（前半使用与 API，后半实现结构与安全），删除 **`分类实现摘要.md`**。
- 建表**唯一**以 Drizzle 为准：删除 **`scripts/init-db.sql`**、**`scripts/数据库初始化脚本说明.md`**；更新 **`docs/文档索引.md`**、**`README.md`**、**`docs/博客系统开发说明.md`** 中相关引用。

---

## 2026-04-10 — Docker Compose 编排与生产构建修复

**类型**：部署 / 类型与构建

### 需求

使用 Docker 对项目进行流水线式部署：MySQL、Redis、应用镜像；提供配置模板与 **`docs/Docker编排与流水线部署.md`**（后续已定为唯一部署说明）。

### 方案摘要

- 新增 **`docker-compose.yml`**（`mysql`、`redis`、`blog-web`）、**`Dockerfile`**、**`Dockerfile.migrate`**、**`deploy/env.docker.example`**、**`.dockerignore`**；**`deploy/README.md`** 与 **`docs/Docker编排与流水线部署.md`**。
- **`next.config.ts`**：`output: "standalone"` 仅在 **`NEXT_STANDALONE=true`** 时启用（**Dockerfile** 构建阶段设置），避免 Windows 下 pnpm symlink 导致 standalone 打包 **EPERM**。
- **`.gitignore`**：保留 **`drizzle/**/\*.sql`**、忽略 **`deploy/.env.docker`\*\*。
- **构建通过**：修复多处 **profile / favorites / notifications / activities / profile POST** 的 Drizzle 与类型问题；**HeroUI** `Button`/`Badge`/`SelectItem` 与 **`getDictionaryForLang`** 等与当前组件库类型对齐。

### 关键路径

`docker-compose.yml`、`Dockerfile`、`Dockerfile.migrate`、`deploy/env.docker.example`、`docs/Docker编排与流水线部署.md`、`next.config.ts`

### 验证

- 本机：`rm -rf .next && pnpm run build` 通过（未设 `NEXT_STANDALONE`）。
- 镜像：在 **Linux** 环境执行 **`docker build`**（内建 `NEXT_STANDALONE=true`）。

---

## 2026-04-10 — 部署文档唯一化与 Compose 默认端口

**类型**：文档 / 运维

### 需求

- 仅以 **Docker Compose 编排与流水线部署** 作为**唯一**部署文档；删除不符合当前流程的 **`部署操作指南.md`**、**`部署实践日志.md`**。
- 默认宿主机端口：**MySQL `13307`**、**Redis `16380`**、**blog-web `13001`**（容器内应用仍为 **3000**）。

### 处理

- 更新 **`docker-compose.yml`**、**`deploy/env.docker.example`**、**`docs/Docker编排与流水线部署.md`**、**`docs/文档索引.md`**、**`README.md`**、**`env.example`**、**`lib/db/本地MySQL-Docker说明.md`**；**`next.config.ts`** 增加 **`localhost:13001`** 的 `images.remotePatterns`。
- 已删除上述两篇旧部署文档。

---

## 2026-04-10 — 修复 semantic-release 提交时 Husky 拦截 CHANGELOG

**类型**：CI / semantic-release

### 问题

`@semantic-release/git` 在 CI 中执行 `git commit` 时仍会安装并触发 **husky** `pre-commit`（`format:check`）。**`@semantic-release/changelog`** 生成的 **`CHANGELOG.md`** 与 Prettier 规则不一致，导致提交失败。

### 处理

1. **`@semantic-release/exec`**：在 **changelog、npm** 插件之后，**git** 提交之前执行 **`prettier --write CHANGELOG.md package.json`**。
2. **`.github/workflows/release.yml`**：在 **semantic-release** 步骤设置 **`HUSKY=0`**，避免发布机器人提交再走本地钩子（与 Prettier 双保险）。

---

## 2026-04-10 — 修复 Release 工作流 pnpm/action-setup 版本冲突

**类型**：CI

### 问题

`pnpm/action-setup@v4` 与根目录 **`package.json` 的 `packageManager`**（`pnpm@9.0.0`）同时指定版本时抛错：_Multiple versions of pnpm specified_。

### 处理

`.github/workflows/release.yml` 中 **去掉** `pnpm/action-setup` 的 `with.version`，由 action **仅从 `packageManager` 解析** pnpm 版本。

---

## 2026-04-10 — GitHub Actions 自动发版（semantic-release）

**类型**：CI / 工程自动化

### 需求

推送到 GitHub **`main`** 且存在可发布提交（如 **`feat`**、**`fix`**、**`perf`**）时，由 CI 运行 **semantic-release**：提升根包 **`version`**、更新 **`CHANGELOG.md`**、创建 **GitHub Release**。提交信息符合仓库 **Angular** 约定（`type(scope): 中文说明`）。本地 **`pnpm run release:dry-run`** 预演。

### 方案

- 安装 **`semantic-release`** 及官方插件（changelog、npm 仅 bump、github、git、analyzer、release-notes-generator）。
- 新增 **`release.config.cjs`**：`private` 包 `npmPublish: false`；`@semantic-release/git` 提交 **`[skip ci]`** 以免循环触发。
- 新增 **`.github/workflows/release.yml`**：`push` → `main`，`pnpm install --frozen-lockfile` 后 `semantic-release`；`GITHUB_TOKEN` + `contents: write`。
- **`package.json`**：增加 **`repository`**、`script` **`release:dry-run`**。

### 关键路径

`release.config.cjs`、`.github/workflows/release.yml`、`package.json`、`docs/自动发版与semantic-release.md`

### 验证

- `pnpm run release:dry-run`：需 **git 远程与完整历史**；若要在本地跑通 GitHub 插件校验，需临时设置 **`GITHUB_TOKEN`** / **`GH_TOKEN`**（见 `docs/自动发版与semantic-release.md`）。
- 合并到 `main` 后，在 **Actions** 中可见 **Release**；有可发布提交时应出现新 tag 与 Release。

### 说明

仓库为**单根包**发版；`.cursorrules` 中「GitLab CI」已改为 **GitHub Actions** 表述以与实现一致。

---

## 2026-04-10 — 优化文档管理（统一变更记录文件）

**类型**：文档流程

### 需求

- 不再为每次变更单独新建变更记录文件。
- 仅维护 **`docs/变更记录.md`**：每次把**最新**变更写在**文档最上面**，按**时间 — 变更标题**分块。

### 处理

- 合并原 `变更记录-20260410-Cursor规则与文档沉淀约定.md`、`变更记录-Git主分支main.md` 的内容至本文件（历史条目见下文分块）。
- 删除上述独立文件；`docs/文档索引.md` 与 `开发过程文档撰写规范.md`、`.cursor/rules/AI协作与文档沉淀.mdc` 改为指向本文件及新约定。

### 验证

- `docs/` 下仅保留一个总览式 **`变更记录.md`**；索引中只链接本文件。

---

## 2026-04-10 — Git 主分支使用 main

**类型**：Git / 工程约定

### 需求

将仓库默认主分支从 `master` 改为 **`main`**。

### 已做操作（本地）

1. **`git branch -m main`**：将当前分支名由 `master` 重命名为 `main`（本仓库当时尚无任何 commit 时亦可执行）。
2. **`git config init.defaultBranch main`**：本仓库本地配置中指定 **`git init` 时默认初始分支为 `main`**。

### 文档与规则

- **`.cursorrules`** 中 semantic-release 相关表述为仅写推送到 **`main`**。

### 远程仓库（若已存在）

若 GitLab/GitHub 上仍只有 `master`，首次 `git push` 后需在托管平台将**默认分支**设为 `main`，并按需删除或归档 `master`。

```bash
git push -u origin main
```

### 验证

- `git status -sb` 显示在 **`main`** 上。

---

## 2026-04-10 — Cursor 规则优化与文档沉淀约定

**类型**：工程规范 / 文档流程

### 需求摘要

- 按项目**当前真实情况**优化 `.cursor` 下规则；规则文件改为**中文文件名**。
- 约定：使用 AI 处理本仓库时，将**用户需求、解决方案、关键代码与配置**写入 `docs/`；需求变化时**同步更新**相关文档。

### 方案说明

1. **删除**原 `.cursor/rules/` 下多份英文命名、内容重复或与仓库不一致的规则。
2. **新建**三份 **`alwaysApply: true`** 的中文规则（`.mdc`）：`AI协作与文档沉淀.mdc`、`项目概览与约定.mdc`、`代码修改与工作流.mdc`。
3. **新增** `docs/开发过程文档撰写规范.md`；**更新** `docs/文档索引.md` 中「Cursor 与协作沉淀」章节。
4. **更新** 根目录 `.cursorrules`：顶部指向 `.cursor/rules/*.mdc` 与 `docs/` 沉淀要求；**Git 提交规范**保持原文完整条款（Angular、semantic-release、示例等）。

### 关键路径

| 路径                                 | 说明                    |
| ------------------------------------ | ----------------------- |
| `.cursor/rules/AI协作与文档沉淀.mdc` | 文档沉淀强制规则        |
| `.cursor/rules/项目概览与约定.mdc`   | 项目事实与约定          |
| `.cursor/rules/代码修改与工作流.mdc` | 修改与验证习惯          |
| `docs/开发过程文档撰写规范.md`       | 撰写模板与维护约定      |
| `docs/文档索引.md`                   | 总索引                  |
| `.cursorrules`                       | 根级指针与 Git 提交规范 |

### 验证

- `.cursor/rules/` 仅含上述三份 `.mdc`，文件名为中文。

---

**维护**：以后每次值得入库的变更，在本文**最上方**新增一块（日期 — 标题），沿用「需求 / 方案或操作 / 验证」结构即可；详细专题仍可单独建 `docs/某主题说明.md`，并在 [文档索引.md](./文档索引.md) 登记。
