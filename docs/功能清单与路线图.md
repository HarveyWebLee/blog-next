# 功能清单与路线图

本文档与**当前仓库实现**对齐；若与旧版手写说明冲突，以 `lib/db/schema.ts`、`app/api` 与页面代码为准。

## 已具备的能力（摘要）

### 认证与账户

- 用户名/邮箱登录、注册、邮箱验证码（JWT + `bcryptjs`）
- 忘记/重置密码链路已接入数据库 token、邮件发送、限流、密码强度校验与活动审计
- 密码字段支持 `passwordTransport`（AES-GCM + RSA-OAEP + HMAC + nonce）传输封装
- 角色：`super_admin` / `admin` / `author` / `user`（权限细节以各 API 校验为准）

### 内容与分类

- 文章 CRUD、状态（`draft` / `published` / `archived`）、可见性、浏览计数等
- 文章编辑：**Toast UI Editor**（见 `components/blog/simple-editor.tsx`），支持 Markdown 类编辑体验
- 分类（含 `parentId` 层级）、标签多对多、公开列表与管理页
- 列表「搜索」：主要为 **数据库 LIKE 等条件**，非 Elasticsearch 级全文检索

### 个人中心

- 资料、文章列表、**收藏**（`/api/profile/favorites`）、**点赞列表**（`/api/profile/likes`）、通知、活动等与 schema 对齐的功能
- 粉丝/关注列表、关注/取消关注、公开用户资料页与通知中心已接入真实 API

### 国际化与 UI

- 三种语言路由 + `getDictionary`；HeroUI + Tailwind 4；明暗主题

### 评论与审核

- 前台 `GET /api/comments?postId=` 读取已通过评论及有回复的软删父评论占位（树形最多两层）；`POST /api/comments` 支持匿名/登录提交与可选 `parentId` 回复根评论
- 博客详情页 `PostComments` 展示嵌套列表与回复入口；软删父评论显示「原评论已删除」；前台不返回邮箱/IP 等隐私字段
- 评论默认 `pending`，命中反垃圾规则（`lib/utils/comment-spam.ts`）时进入 `spam`
- 超级管理员可通过 `/api/admin/comments` 和 `/api/admin/comments/{id}` 执行筛选（含 `deleted`）、单条/批量审核、删除（有子则软删占位），并记录审核/删除活动日志
- 评论提交（非 spam）与审核结果会写入 `user_notifications`：新评论/回复通知文章作者与被回复者；通过/拒绝通知评论作者（须登录身份）

### 媒体与对象存储

- `POST /api/uploads/image` / `DELETE /api/uploads/image` 支持登录用户上传/删除 MinIO/S3 兼容图片对象
- 文章创建/编辑页已接入特色图上传组件，scope 为 `article`；对象键按用户命名空间隔离
- `media` 表已定义，但当前图片上传链路主要直接写 `posts.featured_image` URL，尚未形成通用媒体库后台

### 开发者工具

- 内置 **`/{lang}/api-docs`**，扫描展示 `app/api` 路由（**仅超级管理员**可浏览与拉取元数据；支持 OpenAPI 导出，见 `docs/接口文档系统说明.md`）
- GitHub CI 已覆盖格式、类型、API 路由包装、R0/R1/R2 回归检查、lint 与 API 文档清单一致性

### 数据库层已定义、产品化程度不一

以下能力在 `schema` 中已定义，但产品化深度不一致，需按具体需求评估：

- **评论嵌套展示**：`comments.parentId` 已用于前台两层树；父评论软删 `deleted` 占位已落地；更深树分页仍待做
- **媒体**表 `media`：当前 MinIO 上传不写入通用媒体资产表，媒体库、复用、审计与清理策略仍待设计
- **系统设置**表（后台 UI 与读取路径视需求而定）

## 配置说明

- 根目录 **`env.example`** 仅保留当前已用键；**Redis 等**是否在运行时可查 **`deploy/env.docker.example`** 与 Compose，**是否被业务代码读取**以全局检索为准。

## 待办与改进方向

**集中 backlog 见 [项目TODO.md](./项目TODO.md)**（未完项、技术债、部署预留变量等）。以下为按价值粗略分组摘要：

### 内容与编辑

- [ ] 草稿**自动保存**（前端定时/失焦提交）与冲突处理
- [ ] 将 MinIO 上传与 `media` 表打通，形成通用媒体库、引用关系与清理策略
- [ ] 文章版本历史、导入导出、批量操作（按需）

### 互动与审核

- [x] 评论前台列表与最多两层嵌套回复（读取接口 + UI）
- [x] 评论回复 / 审核站内通知联动（`user_notifications`）
- [x] 更细垃圾过滤策略；删除父评论占位（`status=deleted`）
- [ ] 阅读进度、分享分析等运营指标产品化

### 发现与运营

- [ ] 更强搜索（全文索引、独立搜索服务或 MySQL FULLTEXT 等）
- [ ] RSS、站点地图
- [ ] 管理端「仪表盘」聚合统计

### 性能与可靠

- [x] 文章列表标签已批量加载，消除列表接口的 N+1 查询
- [x] API 全局限流可通过 `RATE_LIMIT_WINDOW` / `RATE_LIMIT_MAX_REQUESTS` 配置，路由级限流规则继续叠加
- [ ] 热点列表缓存可按 Redis 配置逐步接入
- [ ] 图片压缩、懒加载策略统一
- [ ] 监控、审计日志、备份恢复演练

### 安全

- [x] 生产启动时强校验 `JWT_SECRET` / `JWT_REFRESH_SECRET`，避免 fallback 默认值
- [x] API CORS 白名单和预检请求已接入 `middleware.ts`；仅允许 `CORS_ORIGIN` 中的独立前端 Origin
- [x] refresh token 已写入 HttpOnly Cookie；前台不再持久化 refresh；Body 兼容已移除
- [x] 分类/标签写接口与管理入口已收紧为 author/admin/super_admin（服务端 RBAC）
- [ ] 继续收紧高敏运维接口与超级管理员操作审计
- [x] accessToken 与统一客户端请求层收口（减少组件直接读 localStorage）

## 分阶段建议

1. **近期**：会话 Cookie 化和服务端 RBAC；评论前台列表/回复树；媒体资产闭环与草稿自动保存。
2. **中期**：MySQL FULLTEXT 搜索；RSS/站点地图；管理仪表盘；监控、备份恢复演练。
3. **长期**：根据实际使用数据评估文章版本历史、协作编辑、多租户和插件化。

**可执行版本计划**见 [后续需求与功能开发计划.md](./后续需求与功能开发计划.md)，其中包含迭代窗口、验收标准、依赖和风险决策。

## 技术债提示（非穷尽）

- 服务层与页面中仍有较多 `any`，建议先从 API 响应类型、`PostService` join 结果和 profile 组件逐步收紧
- 复杂查询建议配合索引与分页策略复查；文章列表标签已完成批量化，后续优先关注缓存与搜索索引
- 文档、字典与路由的新增语言需同步 `middleware.ts` 与 `generateStaticParams`

---

_最后更新：2026 年 7 月（待办详见 [项目TODO.md](./项目TODO.md)）_
