---
description: 新增或变更 Route Handler / 数据库表时，同步内置 api-docs 元数据与 docs 下接口库表文档
globs:
alwaysApply: true
---

# 接口与数据库文档同步（强制）

在**新增、删除或修改** `app/api/**/route.ts` 中的接口行为时，以及**新增、删除或修改** `lib/db/schema.ts`（或迁移涉及的表结构）时，助手须在同一轮改动中完成下列同步，避免「代码已变、文档与内置文档站仍停留在旧版」。

本规则与 **`.cursor/rules/AI协作与文档沉淀.mdc`** 并行：专题流水见下文；仓库级总览仍可写在 **`docs/变更记录.md`**（新条目置顶），可引用本规则维护的文档。

## 一、与 `/{lang}/api-docs`（@api-docs）相关的配置

内置文档站通过 **`lib/utils/api-scanner.ts`** 扫描 `app/api`，并用 **`lib/utils/api-docs-endpoint-meta.ts`** 合并人工元数据（分组副标题、端点一句说明、鉴权提示）。

当接口有新增或行为/鉴权变化时，**按需**更新：

1. **`lib/utils/api-docs-endpoint-meta.ts`**
   - **`API_DOCS_GROUP_DESCRIPTIONS`**：新分组目录或分组职责变化时。
   - **`API_DOCS_ENDPOINT_DESCRIPTIONS`**：新路径或 HTTP 方法；路径格式须与扫描器输出的 `apiPath` 一致（动态段为 `{id}`、`{slug}`，catch-all 为 `{...path}`）。
   - **`API_DOCS_AUTH_HINTS`**：鉴权方式、Cookie/Bearer、公开/管理员等与调用方相关的约束变化时。
2. 对应 **`route.ts`** 顶部 **JSDoc**（方法摘要、查询参数、请求体要点），便于扫描器与后续阅读。
3. 若 **`dictionaries/*`** 中 `apiDocs` 相关键因界面文案变化而需调整，一并修改。

**说明**：若仅修正实现细节且对外契约未变，可只更新 JSDoc；若路径/方法/鉴权变更，必须更新元数据与下文 **`docs/接口文档.md`**。

## 二、接口清单与接口变更流水（`docs/`）

| 文档 | 用途 |
|------|------|
| **`docs/接口文档.md`** | **全量接口清单**：按分组列出路径、HTTP 方法、实现文件、简述、鉴权要点等可追溯信息；以当前仓库 `app/api` 为准维护。 |
| **`docs/接口变更文档.md`** | **接口专项变更流水**：每次接口增删改在文件**最上方**新增一块（日期 — 标题），写清变更点、涉及文件、对客户端影响；并**回写** `docs/接口文档.md` 中对应条目。 |

每次接口相关提交应：**先改代码与元数据，再更新 `接口文档.md`，再在 `接口变更文档.md` 顶部追加记录**。

## 三、数据库表清单与表结构变更流水（`docs/`）

| 文档 | 用途 |
|------|------|
| **`docs/数据库表记录文档.md`** | **全量表清单**：表名、Drizzle 导出名、用途、主键与关键字段、重要索引或枚举；以 **`lib/db/schema.ts`** 与已应用迁移为权威来源。 |
| **`docs/数据表变更文档.md`** | **表结构专项变更流水**：每次执行 `db:generate` / `db:migrate` / `db:push` 等导致表定义变化时，在文件**最上方**新增一块，说明迁移文件、前后差异、回填或兼容注意；并**回写** `数据库表记录文档.md**。 |

## 四、执行顺序（建议）

1. 实现或修改 `route.ts` / `schema.ts`（及迁移）。
2. 更新 **`api-docs-endpoint-meta.ts`** 与路由 JSDoc（若适用）。
3. 更新 **`docs/接口文档.md`** 或 **`docs/数据库表记录文档.md`**。
4. 在 **`docs/接口变更文档.md`** 或 **`docs/数据表变更文档.md`** 顶部追加本次说明。
5. 更新 **`docs/文档索引.md`**（若新增或废弃上述文档类型时）。
6. 视需要在本任务对应的 **`docs/变更记录.md`** 顶部增加提要条目。

## 五、可排除情形

- 用户**明确**声明本次不改 `docs`、不改元数据时，以用户说明为准，但须在对话中确认可能产生的文档滞后风险。
