---
description: 使用编辑工具改代码；小步验证；遵守 lint 与预提交
globs:
alwaysApply: true
---

# 代码修改与工作流

## 修改方式

- **优先使用 Cursor/IDE 提供的编辑能力**（对仓库文件直接打补丁），避免把整文件长代码块甩给用户手抄。
- 改动前先**阅读**待改文件及邻近调用处，**对齐**现有命名、类型与注释风格。
- **控制范围**：仅改与当前需求相关的文件与行；避免顺带大重构。

## 验证

- 逻辑或类型敏感时：运行 **`pnpm lint`**；提交前环境可运行 **`pnpm precommit`**（等价 `format:check` + `lint-staged`）。
- 涉及 DB schema 时：提醒执行 `db:generate` / `migrate` 等与团队一致的流程。

## 国际化与可访问

- 用户可见文案：走 **`dictionaries`** 三语键（或项目既定模式），避免硬编码。
- 站内链接：使用 **`next/link`** 及正确 **`/{lang}/...`**（前台路由）。

## 与文档的绑定

任何**非琐碎**的功能、配置或排错结论，须按 **`.cursor/rules/AI协作与文档沉淀.mdc`** 写入 `docs/` 并维护索引。

**接口与数据库表**：新增或变更 **`app/api`**、**`lib/db/schema.ts`** 时，须按 **`.cursor/rules/接口与数据库文档同步.mdc`** 同步 **`api-docs` 元数据**与 **`docs/接口文档.md`**、**`docs/数据库表记录文档.md`** 等（详见该规则）。
