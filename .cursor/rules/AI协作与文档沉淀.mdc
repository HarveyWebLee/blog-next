---
description: 需求与方案必须写入 docs；变更时同步更新文档
globs:
alwaysApply: true
---

# AI 协作与文档沉淀（强制）

本规则在**使用 Cursor 处理本仓库任意需求时**始终适用。

## 必须遵守

1. **将用户提出的需求**（目标、约束、验收口径）整理为可追溯的文字。
2. **将本助手给出的解决方案**（选型理由、步骤、取舍）写清楚。
3. **记录关键代码与配置**：文件路径、Export/组件名、环境变量键、`package.json` 脚本、ESLint/Husky 等**可检索线索**；大段重复源码可概括 + 指向路径，避免无意义粘贴。
4. **全部写入 `docs/` 目录**下文档（中文文件名），不得在仅聊天中「说完就算」。
5. **若后续需求变化或实现调整**：在改代码的**同一轮修改**内，把对应文档**更新到与当前行为一致**；若文档已不再适用，在文档内标明废弃或删除并更新「文档索引」。

## 文档落位

- **总体规范与模板**：见 `docs/开发过程文档撰写规范.md`。
- **索引维护**：新建或废弃专题文档时，必须更新 `docs/文档索引.md` 对应表格或章节。
- **变更流水**：值得入库的每次变更，在 **`docs/变更记录.md` 最上方** 新增一块（**日期 — 标题**），见该文件文首约定；勿再散落多个 `变更记录-*.md`。
- **专题说明**：长期能力或复杂功能仍用独立中文文件名，如 `docs/xxx功能说明.md`（与现有文档风格一致），并更新索引。
- **接口与数据表**：涉及 `app/api` 或 `lib/db/schema.ts` 时，还须遵守 **`.cursor/rules/接口与数据库文档同步.mdc`**（`docs/接口文档.md`、`docs/数据表变更文档.md` 等）。
- **沟通语言**：助手默认简体中文；文档正文使用简体中文。

## 可排除情形（极少）

- 用户**明确**说「本次不要写文档 / 不要动 docs」——则跳过，但需在对话中简要确认。
- 纯只读答疑、且**未**改仓库任何文件时，可不新增文件；若仍涉及重要结论且用户希望保留，可询问是否在 **`docs/变更记录.md` 顶部** 追加一块。

## 与代码修改的配合

文档工作应作为**同一任务收尾的一部分**：先/并行完成实现，再写入或更新 `docs/`，避免事后脱节。
